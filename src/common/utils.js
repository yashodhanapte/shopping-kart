/**
 * Check whether array length is greater than or equal to given limit
 * @param {*} arr - The input array to check
 * @param {*} limit - The limit to check array's length with
 * @returns Boolean
 */

export const checkIfArrayLengthMoreThanLimit = (arr, limit) => {
    return arr.length >= limit ? true : false;
}